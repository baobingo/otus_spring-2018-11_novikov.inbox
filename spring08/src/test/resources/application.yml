spring:
  data:
    mongodb:
      database: 777_books-test
      host: localhost
      customseq: customSequences
logging:
        level:
          org.springframework.data.mongodb.core.MongoTemplate: DEBUG